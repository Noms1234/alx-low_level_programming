#include "main.h"
/**
* positive_or_negative - Checks number is positive negative or zero
* @i: the number is checked
* Return: always 0
*/

void positive_or_negative(int i)
	{
		if (i < 0)
			printf("%d us negative", i);
		else
			printf("%d is positive", i);
	}
